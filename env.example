# Database connection
DB_HOST=localhost
DB_PORT=23798
DB_NAME=squid
DB_USER=postgres
DB_PASS=postgres

# RPC endpoint (optional - uses Subsquid archive by default)
# RPC_OPTIMISM_HTTP=https://mainnet.optimism.io

# GraphQL server port (optional, defaults to 4000)
# GQL_PORT=4000


